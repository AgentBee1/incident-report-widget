// AgentBee Incident Report Widget — Environment Configuration
// Deploy to Vercel and set this as an environment variable, OR replace the value below.

// Option A: Vercel environment variable (recommended)
// In Vercel dashboard: Settings > Environment Variables
// Name: VITE_SUPABASE_ANON_KEY  (or just paste directly below)

// Option B: Direct injection for simple static hosting
// Replace 'YOUR_SUPABASE_ANON_KEY' in index.html where SUPABASE_KEY is set,
// OR use the window.SUPABASE_ANON_KEY pattern below in a separate env.js file:

// window.SUPABASE_ANON_KEY = 'eyJ...your_key_here...';

// SUPABASE URL is already hardcoded: https://mtatzzrbapftzwuxrcko.supabase.co
